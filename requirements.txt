# VUCA Agricultural Sentiment — Python Dependencies
# Install: pip install -r requirements.txt

# Data loading and manipulation
pandas>=2.0
numpy>=1.24
openpyxl>=3.1          # read xlsx files

# Statistics
scipy>=1.11            # Hartigan dip test (diptest), kurtosis, etc.
statsmodels>=0.14      # VAR, Granger causality tests
scikit-learn>=1.3      # PCA, LDA preprocessing, LatentDirichletAllocation

# Topic modeling
gensim>=4.3            # alternative LDA if needed

# Text processing
nltk>=3.8              # tokenization, stopwords
textblob>=0.17         # polarity scoring

# Visualization
matplotlib>=3.7
seaborn>=0.12

# File formats
pyarrow>=12.0          # parquet read/write (used by pandas)

# Optional: diptest package for Hartigan dip test
# If not available via scipy, install separately:
# diptest>=0.6
